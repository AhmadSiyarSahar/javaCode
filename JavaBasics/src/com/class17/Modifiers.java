package com.class17;

public class Modifiers {

	public static void main(String[] args) {
		// Two types of modifiers
		// 1- Access Modifier : public, protected, default, private
		// 2- non access modifiers: static, final, abstract 
		
		// public class: accessible to the enitire project. 
		// protected: accessible to all classes in the same package. 
		// default :accessible to all classes in the same package. 
		// private : accessible within the same class only. 
		
		// note having accessModiers and non accessModiers are optional 
		
		// Formula for method with access modifiers
		// optional 		optional		must 		must 		optional
	// AccessModifier	non accessModiers  returnType methodName (parameterList);
		
		// Formula for variable 
		// optional 		optional			 MUST			Must
	// AccessModifier   non accessModiers		dataType 	nameOfMethod
		
		

	}

}
